// Admin entry point. Stimulus is loaded for targeted progressive-enhancement
// controllers (e.g. variant_preview). Turbo is not used in admin.
import "controllers"
import "admin_store_receipt_messages"
