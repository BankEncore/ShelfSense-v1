# frozen_string_literal: true

module Admin
  # Disposable UDS-5.1 composition fixture. Signed-in only. Retire after Product
  # slices consume the primitives, or in UDS-5.5.
  class Uds5CompositionPrototypesController < BaseController
    def show
    end
  end
end
