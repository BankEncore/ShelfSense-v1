<%#
  locals: groups:, identity: (ShiftEndTape::Identity or Array)
%>
<% tape_lines = Pos::ShiftEndTape.lines(groups: groups, identity: identity) %>
<section class="pos-shift-end-tape" aria-hidden="true">
  <pre class="pos-shift-end-tape__pre"><%= tape_lines.join("\n") %></pre>
</section>
